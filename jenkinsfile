
pipeline {
    agent any

    stages {
        stage('Clone Repository') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/USERNAME/REPO.git'
            }
        }

        stage('Run PHP File') {
            steps {
                powershell 'php index.php'
            }
        }
    }
}
